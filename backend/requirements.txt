# ----- Web framework -----
fastapi==0.115.5
uvicorn[standard]==0.32.1
python-multipart==0.0.18
pydantic==2.10.3
pydantic-settings==2.6.1

# ----- OpenAI / AI -----
openai==1.57.0

# ----- RAG / vector store -----
faiss-cpu==1.9.0
numpy==2.1.3
langchain-text-splitters==0.3.2

# ----- Document ingestion -----
PyMuPDF==1.25.1          # PDFs (fitz)
python-docx==1.1.2       # Word .docx (read + write/export)
openpyxl==3.1.5          # Excel .xlsx/.xls
python-pptx==1.0.2       # PowerPoint .pptx
Pillow==11.0.0           # image handling for OCR
pytesseract==0.3.13      # OCR (requires system `tesseract` binary)

# ----- Export -----
reportlab==4.2.5         # PDF generation

# ----- Tooling / tests -----
pytest==8.3.4
httpx==0.28.1
